let full_name:string = "Eric John"
console.log("Lower Case:", full_name.toLowerCase());
console.log("Upper Case:", full_name.toUpperCase());
console.log("Title Case:", full_name.charAt(0).toUpperCase()+full_name.slice(1).toLowerCase());
